# railway.toml — for Railway.app deployment
[build]
builder = "dockerfile"

[[services]]
name = "backend"
source = "backend"

[services.deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"

[[services]]
name = "frontend"
source = "frontend"
